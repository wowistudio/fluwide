'use client';

import NavDesktop from './NavDesktop';

export default () => {
    return (
        <header className="absolute left-0 w-full px-4 z-50">
            <NavDesktop />
        </header>
    )
}